import { BookingComponent } from "../booking/booking.component";
import { AuthguardService } from "../services/authguard.service";

export const bookingRoutes = [
    {path:'Flight',component:BookingComponent}
]